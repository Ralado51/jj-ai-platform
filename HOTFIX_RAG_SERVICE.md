# Hotfix — RAG service syntax

This hotfix repairs invalid Python introduced while resolving conflicts between the RAG observability and AI Workspace source changes.

It restores valid indentation, keeps the enriched source metadata, preserves observability metrics, removes the obsolete duplicate citation-normalization call, and adds a syntax regression test.
